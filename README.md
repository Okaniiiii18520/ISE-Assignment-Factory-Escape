# speedinggrinch — Pygame template

A small Pygame template demonstrating movement inspired by SpeedRunners.

Setup

```bash
python -m pip install -r requirements.txt
```

Run

```bash
python -m src.main
```

Controls

- Left / Right: Move
- Space: Jump (hold for longer jump)
- Shift: Dash (short burst, cooldown)
